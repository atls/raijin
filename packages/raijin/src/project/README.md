# Project metadata

Reads project manifests, project kind and workspace package names through the retained Yarn project model. This capability does not install dependencies or select an alternative project root for a command invocation.

Consumers use `@atls/raijin/project`. Run `yarn test unit --target packages/raijin/src/project` for single-package and workspace project cases.
