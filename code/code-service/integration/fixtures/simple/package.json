{
  "name": "@fixtures/service-simple",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn service build",
    "dev": "yarn service dev",
    "prepack": "run build"
  }
}
