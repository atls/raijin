{
  "name": "@atls/code-format",
  "version": "0.0.10",
  "license": "BSD-3-Clause",
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn library build",
    "build:worker": "rollup -c",
    "prepack": "yarn run build",
    "postpack": "rm -rf dist"
  },
  "dependencies": {
    "@atls/config-prettier": "workspace:0.0.2",
    "@atls/prettier-plugin": "workspace:0.0.2",
    "globby": "^11.0.4",
    "ignore": "^5.2.0",
    "prettier": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.10"
  },
  "publishConfig": {
    "main": "dist/index.js",
    "typings": "dist/index.d.ts"
  }
}
