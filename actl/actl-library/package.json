{
  "name": "@atls/actl-library",
  "version": "0.0.6",
  "license": "BSD-3-Clause",
  "main": "src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "builder build library",
    "postpack": "rm -rf dist",
    "prepack": "yarn run build"
  },
  "devDependencies": {
    "@atls/tools-builder": "workspace:0.0.0",
    "@types/node": "^16.7.10",
    "@types/rimraf": "^3.0.2"
  },
  "dependencies": {
    "@atls/code-typescript": "workspace:0.0.5",
    "clipanion": "^3.0.1",
    "rimraf": "^3.0.2"
  },
  "publishConfig": {
    "access": "public",
    "main": "dist/index.js",
    "typings": "dist/index.d.ts"
  }
}
