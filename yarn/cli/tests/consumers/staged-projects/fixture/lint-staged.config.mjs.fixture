export default {
  'backend/**/*.{js,ts}': () => ['yarn typecheck', 'yarn test unit backend/value.test.js'],
  'client/**': (files) => {
    if (files.length > 0) {
      throw new Error('client requires its own lint-staged configuration')
    }

    return []
  },
}
