# Yarn builder

TODO: switch back to upstream `@yarnpkg/builder` once it supports ESM bundle output and external files.

Until then, Raijin uses the thin builder package fork from `https://github.com/TorinAsakura/yarnpkg-builder` and pins it from the root `package.json`.

Tracking:

- upstream issue: https://github.com/yarnpkg/berry/issues/7200
- upstream pull request: https://github.com/yarnpkg/berry/pull/7201
