{
  "$schema": "http://json-schema.org/schema",
  "$id": "RaijinProjectScaffold",
  "title": "Generate a Raijin project scaffold",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "type": {
      "description": "The scaffold type.",
      "type": "string",
      "enum": ["project", "library"],
      "default": "project"
    }
  },
  "required": ["type"]
}
