name: Reusable version

on:
  pull_request:
    types:
      - closed
    branches:
      - master

jobs:
  run:
    name: Version
    env:
      NODE_VERSION: '19'
    runs-on: ubuntu-latest

    steps:
      - uses: atls/tools/.github/actions/setup@master
        with:
          REF: master

      - name: Version
        uses: atls/tools/.github/actions/version@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit changes
        uses: atls/tools/.github/actions/commit@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
