name: Publish to NPM

on:
  workflow_dispatch:

jobs:
  run:
    name: Publish
    env:
      NODE_VERSION: '19'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: master

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install
        uses: atls/tools/.github/actions/install@master

      - name: Publish
        uses: atls/tools/.github/actions/publish@master
        with:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Commit changes
        uses: atls/tools/.github/actions/commit@master
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
